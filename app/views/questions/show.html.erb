<h1><%= @question.title %></h1>
<%= @question.content %><br>
回答で<%= @question.have_point %>ポイントゲット！
<%= @question.period %>まで！


<%= form_for @answer, url: questions_show_path do |f| %>
  <% if @question.style_id == 1 %>
    <%= f.label :content %>
    <%= f.text_field :content %>
    <%= f.submit name: "#{@question.id}" %>
  <% else %>
    <% @question.selects.each do |q| %>
      <%= f.submit "#{q.choices}", name: "#{q.id}" %>
    <% end %>
  <% end %>
<% end %>